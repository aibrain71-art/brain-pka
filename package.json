{
  "name": "brain-pka",
  "version": "1.0.0",
  "private": true,
  "description": "Personal Knowledge Assistant — Cloudflare Pages app",
  "scripts": {
    "test": "playwright test",
    "test:debug": "playwright test --debug=cli",
    "test:report": "playwright show-report",
    "test:local": "BASE_URL=http://localhost:7823 playwright test"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
